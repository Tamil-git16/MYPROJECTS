
# Allow CORS from the React frontend
CORS_ALLOWED_ORIGINS = [
    "http://localhost:3000",  # React dev server
]

# Optional: Allow credentials (cookies or authentication headers)
CORS_ALLOW_CREDENTIALS = True
